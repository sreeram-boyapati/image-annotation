project(src)
cmake_minimum_required(VERSION 2.8)
aux_source_directory(. SRC_LIST)
find_package( OpenCV REQUIRED )
find_package(Boost 1.54.0 COMPONENTS system)
include_directories( ${OpenCV_INCLUDE_DIRS} )
#MongoDB Drivers
Message(${OpenCV_LIBS})
if(Boost_FOUND)
    include_directories(${Boost_INCLUDE_DIRS})
endif()
add_executable(segment_images segment_images.cpp)
target_link_libraries( segment_images ${OpenCV_LIBS} )
target_link_libraries(segment_images ${Boost_LIBRARIES})
target_link_libraries(segment_images "/usr/local/lib/libmongoclient.so")
